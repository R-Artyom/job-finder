<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
    <!-- Фон в стиле терминала Ubuntu -->
    <rect width="64" height="64" rx="12" fill="#2C001E"/>

    <!-- Крупный текст JF -->
    <text
            x="50%"
            y="52%"
            font-size="50"
            font-family="'Ubuntu Mono', monospace"
            font-weight="bold"
            text-anchor="middle"
            fill="#E95420"
            dominant-baseline="middle"
    >
        JF
    </text>
</svg>
